export enum VehicleStatus {
    IDLE = 'idle', // Ocioso (Disponível)
    IN_TRANSIT = 'in_transit', // Em Trânsito (Em uso)
    MAINTENANCE = 'maintenance', // Manutenção
    OUT_OF_SERVICE = 'out_of_service', // Fora de Serviço
}